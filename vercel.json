{
  "version": 2,
  "builds": [
    {
      "src": "cmd/rest/main.go",  // File entrypoint REST API
      "use": "@vercel/go"         // Builder untuk Go
    }
  ],
  "routes": [
    {
      "src": "/(.*)", 
      "dest": "/"     // Arahkan semua request ke Go server
    }
  ]
}
